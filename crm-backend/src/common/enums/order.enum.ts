export enum ORDER_TYPE {
  SALES = 'sales',
  SERVICE = 'service',
  SUBSCRIPTION = 'subscription',
}

export enum ORDER_STATUS {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
